# ATTENTION : version EPINGLEE volontairement. `scripts/patch_streamlit_pwa.py`
# (mode "appli iPhone/tablette" : icone d'ecran d'accueil, plein ecran, manifest
# PWA) modifie le fichier `index.html` compile a l'interieur du paquet streamlit
# lui-meme. Cette structure interne n'est couverte par aucune garantie de
# compatibilite : une montee de version peut la changer. Le patch etant
# defensif, il n'echouerait pas -- il ne ferait simplement PLUS RIEN, en
# silence, et l'appli perdrait son apparence native sans le moindre message
# d'erreur. Avant de relever cette version, rejouer le build Docker et verifier
# que le log affiche bien "[patch_streamlit_pwa] OK".
streamlit==1.62.0
# Requis par `st.login()` / `st.user`, sur lesquels repose le controle
# d'acces de l'appli (voir services/app_auth.py). Streamlit ne l'installe
# pas de lui-meme : sans cette ligne, la connexion echoue au moment du clic.
Authlib>=1.3.2
pydantic
folium
streamlit-folium
geopy
msal>=1.30.0
requests>=2.31.0
python-docx
Pillow
pandas
openpyxl
# Windows/Mac uniquement (pilote Word) : sur Streamlit Community Cloud
# (Linux, pas de Word), ce marqueur evite d'essayer de l'installer -- la
# conversion PDF y passe automatiquement par LibreOffice (packages.txt),
# voir services/pdf_service.py.
docx2pdf; sys_platform == "win32" or sys_platform == "darwin"
